{
  "name": "@zeroness/agent",
  "version": "0.1.0",
  "description": "zeronessd — in-sandbox agent: verifies signed commands, serves capability I/O, heartbeats.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "zeronessd": "src/zeronessd.mjs" },
  "files": ["src"],
  "scripts": { "test": "vitest run" }
}
